# FileBrowser 共享网盘

> 在云服务器上用 **FileBrowser + nginx** 把本地大文件（4K 电影、Adobe 安装包等）分享给朋友的完整方案。
> 本项目是可复现的配置存档 + 部署文档 + 操作日志，**不含任何真实密码 / 服务器 IP / 个人信息**。

## 它能做什么

- 网页网盘，浏览器直接上传 / 下载 / 在线看文件
- **强制简体中文**（连登录前的页面也是中文，见 `docs/03-中文界面补丁.md`）
- **公私隔离**：`公共/` 目录对 guest 只读开放，`私人/` 只有自己能看到（scope 隔离，越权访问返回 404）
- 登录时用户名 / 密码**自动去掉首尾空格**（防止手滑复制多打空格）
- **单个文件 = 源格式下载**（不压缩）；**文件夹 / 多选 = 打包成 zip** 后下载（无损）
- 支持 GB 级大文件上传（断线续传方案见 `docs/04-大文件上传经验.md`）

## 技术栈

| 组件 | 说明 |
|------|------|
| FileBrowser 2.63.23 | 网页文件管理器（Docker） |
| nginx | 对外反向代理（8081），内网回源 8085 |
| fbproxy.py | 登录空格过滤器（Python，8086，systemd 常驻） |
| Docker | 容器运行 FileBrowser |

> ⚠️ FileBrowser 官方已于 2026-09-01 归档停止维护。当前镜像可用但不再更新；如需长期维护可考虑社区 fork（本项目暂用官方 latest，锁定版本更稳）。

## 目录结构（网盘内）

```
顶层
├── 公共/                  ← guest 只读可见
│   ├── 使用说明.txt
│   ├── Video/电影/触不可及/…
│   └── 常用软件/
│       ├── Adobe/          (ps、Pr 2024…)
│       ├── 学习办公/  常用工具/  游戏/  编程开发/  网络下载/  虚拟机/
└── 私人/                  ← 仅 admin
```

## 账号模型

| 账号  | 权限范围 | 能力         | 用途         |
|-------|----------|--------------|--------------|
| admin | `/`（全部） | 全部（含上传/整理） | 你自己     |
| guest | `/公共`   | 只读         | 分享给朋友 |

> 真实密码保存在服务器本地凭据文件 / 密码管理器，**绝不写入本仓库**。

## 仓库结构

```
├── README.md
├── CHANGELOG.md                  # 变更日志
├── records/                      # 操作记录（做了什么，脱敏）
├── docs/
│   ├── 01-架构说明.md
│   ├── 02-部署与配置.md
│   ├── 03-中文界面补丁.md
│   ├── 04-大文件上传经验.md
│   └── 给朋友的使用说明.md
└── config/
    ├── nginx-site.conf           # 实际 nginx 反代配置
    ├── fbproxy.py                # 登录空格过滤器源码
    ├── filebrowser-docker.txt    # Docker 挂载 / 端口
    └── filebrowser-users.txt     # 用户 / 权限命令模板
```

## 发布前检查

- [ ] 全局搜索，确认无 IP、密码、学号等敏感字段
- [ ] 若公开仓库，可用 Gitee / GitHub；国内访问选 Gitee 更稳
- [ ] 授权方式（开源协议）发布前决定

---
*License：发布前待定。*
