# 操作记录 · 2026-09-06

> 脱敏留痕:记录"做了什么",不含密码 / 服务器 IP / 个人信息。
> 本次主题:**网盘主服务从 47 迁移到本地 Windows(E:\NetDist 大容量仓库),47 改为纯公网入口(frp 隧道)**。

## 背景

- 本地 E:\NetDist 有 ≈537G 内容(老友记全季 4K + 软件包,5177 个文件),47 轻量 40G 盘装不下
- 决定:FileBrowser 主服务跑本地,47 保留 nginx/fbproxy/i18n 补丁 + frps 当入口;访问地址与账号不变

## 47 侧操作

- 部署 `frps` v0.71.0(/usr/local/bin + /etc/frp/frps.toml + systemd,Restart=always)
- nginx `pan` 站点:`location /` 回源 8085(Docker 容器)→ 8087(frp 数据口);`/api/login`→fbproxy 与 i18n alias 两条规则不动
- `fbproxy.py`:BACKEND 8085→8087、转发 Host 头同步;重启
- 备份 db(`/srv/fb/filebrowser.db`)→ 本地;旧 filebrowser 容器 `--restart=no` + stop(数据留 /srv 冷备)
- **踩坑**:阿里云轻量控制台后加的防火墙规则(TCP 7000、换 7500 亦然)显示"已启用"但外网/手机流量均不通;系统预置 443 正常 → frp 控制端口改用 **443**(详见 docs/05)

## 本地 Windows 侧操作

- 目录:网盘根 `E:\NetDist` 建顶层 `公共/`(移入 Software installation package、老友记,补 使用说明.txt)与 `私人/`(空)
- 运行目录 `D:\Tools\fbwin\`:filebrowser.exe(2.63.23,db 为 47 拷贝,账号/guest scope/zh-cn 全保留)、frpc.exe(0.71.0,frpc.toml `loginFailExit=false`)
- filebrowser 监听 127.0.0.1:8085,root=E:\NetDist
- NSSM 注册 `FileBrowserSvc` + `FrpcSvc`(Automatic + 崩溃 5s 重启)

## 实测验证(全链路,浏览器→47:8081→隧道→本地)

- guest 带空格用户名密码登录成功(去空格链路 OK);登录页中文补丁生效
- guest scope=/公共:越权访问 `/私人` → 404
- **单文件下载 Range 支持**:`Range: bytes=1048576-2097151` → `206 Partial Content` + `Content-Range: …/2615192587`
- **断链自愈**:停 47 frps 8s 重启,frpc ~5s 自动重连;断前/断后两段下载拼接后与源文件 **md5 一致**
- 下载速度实测 ≈4.3MB/s(校园网上行瓶颈,预期内)

## 待办 / 备忘

- [ ] **外部目录"指针"接入(用户需求)**:开启 `filebrowser config set --followExternalSymlinks=true` → 重启 FileBrowserSvc 生效(断下载几秒)→ 在 `E:\NetDist\公共\`、`私人\` 下建 junction(`New-Item -ItemType Junction`,免管理员)指向电脑上其他位置(公共/私人两类都有)。⚠️ guest 可顺 公共 下链接访问其目标,公共里只放愿意公开的;安全边界见 docs/05 或说明。实测:不开此开关时指向 root 外的 junction 被 FileBrowser 当"文件"拒进(500)
- [ ] 阿里云防火墙"后加规则不生效"问题:必要时重启实例或提工单;未来若 443 要让给 HTTPS,需要把 frp 挪端口并处理规则异常
- [ ] 47 冷备数据(29G)确认无需回迁后可清理腾盘
- [ ] FileBrowser 升级需重做 i18n 补丁(hash 文件名变化)
- [ ] 公开仓库发布前做全库敏感词扫描(本次新增 config/frpc.toml 等含 `<SERVER_IP>` 占位,发布前再核)
- [ ] 07:54 曾有 `AcrobatDCx64Upd2500120458.msp` 上传到 Software installation package 中途失败(来源非本机,待确认是否本人操作)

## 追加 · 多站点平台 + 指针 + 一键部署(同日,详见 docs/06)

- 需求:47 出网只服务一台电脑浪费 → 做成"站主 + 每终端独立网盘":任意 Windows 终端跑自己 FileBrowser+frpc 接入 47,彼此隔离、各管 guest;站主命令行统一管理。
- **踩坑根治**:新端口外网不通,排查多轮(以为阿里云新增规则不生效/重启实例无效)→ 真相是 **47 系统内 ufw 默认 deny**,只放行了老端口。修复:`ufw allow 7000:7010/tcp` + `8082:8090/tcp`。教训:阿里云轻量有"云防火墙 + 系统 ufw"两层,先查 ufw。
- 47 侧:`/usr/local/bin/pan-ctl`(add-site/ls/stop/start/reset-token/rm-site/show);注册表 `/etc/frp-sites/sites.conf`(root 600);fbproxy 参数化(每站一实例);每站 frps-<站> 独立端口+token;nginx 每站 listen 8080+N;公网 IP 存 `/etc/frp-sites/public_ip`。
- 客户端:`config/deploy/deploy-site.ps1` 一键部署(自动提权;config init→建 admin→可选开指针→注册 FB-<站>/FRP-<站> 服务→断线自愈)。
- **本机模拟"站点2 demo"全链路实测通过**:公网 `:8082` health/登录/目录/Range;服务化 Running;与站1(8081)互不影响。
- **指针实测结论**:FileBrowser 开 followExternalSymlinks 后,**符号链接 `mklink /D` 列目录+下载都正常(200);junction 会 500 只能下载**——指针请用符号链接。
- 站1 回归:health/guest 登录/Range 206 均正常(未被多站建设影响)。
- 遗留:阿里云控制台"后加规则不生效"系 ufw 所致(云层其实生效)——已在 docs/06 记录排查法;demo 站为模拟,接真实朋友时用 pan-ctl add-site 发凭据 + deploy-site.ps1。

## 追加 · 产品化(阶段二):邀请码安装包 + owner 门户(同日,详见 docs/07)

- 新增 47 `pan-web.py`(systemd,127.0.0.1:9100):owner 门户(总览/发邀请/停启删)+ 一次性邀请码 redeem API;nginx `pan-owner`(8089)只暴露 redeem 端点;owner 密码存 `/etc/frp-sites/owner.hash`。
- 邀请码:PAN-xxxxxxxx,24h 有效、一次性(二次兑换被拒实测)。redeem → pan-ctl add-site 自动分端口返回凭据。
- 新增客户端 `install-site.ps1`(UTF-8 BOM,中文向导):输邀请码→兑换→选文件夹→自动生成自足网盘(公共/私人 + _面板 + 启动/停止/查看状态.bat + 指针助手.bat + 使用说明.txt)→ 注册 FB-<站>/FRP-<站>。
- 指针按用户意见改为**默认开启**,不再询问;挂载具体目录用网盘里的 指针助手.bat。
- pan-ctl 站号上限 8(HTTP 8082..8088,预留 8089 redeem 口);站号自动复用空位。
- 实测:模拟"朋友"装站 palsim(8084)全链路 OK;门户 SSH 隧道登录/看站/停启(demo 502↔200)OK;邀请码二次拒;指针(外部文件夹 list+download 200)OK。
- 站1/demo/netdist2 回归正常。

## 追加 · 收尾(门户可下载安装包 + 清理测试站 + 桌面入口)

- 门户页顶部加「下载给朋友的安装包」(`/srv/pan-dist/pan-install.zip`,约 22MB,经 SSH 隧道鉴权下载)。打包含 install-site.ps1 + filebrowser/frpc/nssm;更新包覆盖该文件即可。
- 删除模拟测试站 demo(8082)/palsim(8084)及本地服务与临时目录(D:\site-demo、palsim-netdisk、external-demo 等);保留 main(8081/E:\NetDist)与 netdist2(8083/E:\NetDist2)。
- 桌面新增「看门户.bat」:一条命令开 SSH 隧道并打开 localhost:9200 门户。

## 追加 · 收官(全链路复查 + 清理 + 凭据 + 文档)

- 门户:owner 密码更新;新增"邀请码作废"按钮(删除邀请);清理全部测试邀请码,保留三个真实未用邀请(Sus1ee/lin/Alrisha)。
- main(8081/E:\NetDist)新增两个只读公共账号:Sus1ee、lin(权限仅下载,scope /公共)。
- 站点内"管理工具"统一子目录(启动/停止/查看状态/指针助手),模板收在 config/deploy/pan-tools/;指针助手升级:支持挂文件或文件夹、可到任意多级子目录并自定义名、可列出/删除、往复循环菜单。
- bat 换行/编码教训:cmd 批处理必须 CRLF;含中文必须 GBK(936);install-site 生成后对 *.bat 做 CRLF/去 chcp 后处理(-Recurse)。多次因 LF/UTF8 导致闪退/乱码,已根治。
- 删除 testa 测试站及本地服务/目录;运行时清理;凭据统一记入 runtime\fbwin\凭据-本地备忘.txt(git 忽略)。
- README 全面重写为"一次读懂"版本;docs/07 增补。全库脱敏复查后推 gitee/github。
