# —— 凭据与环境，禁止入库 ——
credentials.txt
*.secret
.env
secrets/
*.key

# —— 运行实例(真身):真实 IP/token/密码/db,物理隔离在 git 之外 ——
runtime/
# 若日后把真实密码备忘放这里，务必忽略：
密码*.txt

# —— 系统文件 ——
Thumbs.db
.DS_Store
desktop.ini

# —— 传输临时文件 / 日志 ——
pr_part_*
*.output
/tmp/
